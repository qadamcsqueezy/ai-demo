# Testing Conventions

No test suite is present in this codebase. No test files, no test runner configured in `package.json`.

If tests are added, place them adjacent to source files or in a `__tests__/` sibling directory following the component/module structure in `src/`.
